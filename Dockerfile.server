# Game server image — built from the repo root so it can see shared/
FROM node:22-alpine
WORKDIR /app

COPY server/package.json server/package-lock.json server/
RUN cd server && npm ci

COPY shared shared
COPY server server

ENV NODE_ENV=production
EXPOSE 8787
CMD ["npm", "--prefix", "server", "run", "start"]
